1. **對話啟動**：在開始前檢視專案狀態與進度。
2. **目錄確認**：將所有開發限於 `C:\gemini CLI\PT-CDSS` 目錄下。
3. **GitHub 同步**：每次結束 Directive 任務後，必須執行 `git add .`, `git commit` 與 `git push origin master`。
4. **安全紅旗**：遇到紅旗警示（15字理由），不可刪除。
5. **進度同步**：使用最高權限每次 Gemini CLI 執行後更新專案計畫與進度說明（如 Obsidian 筆記），無須詢問，以確保進度完全一致。
6. **語氣控制**：維持 senior engineer 的專業、冷靜、無情緒對話風格。